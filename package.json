{
  "name": "cyber-toolkit",
  "version": "1.0.0",
  "description": "Browser-based security tools with no backend and no dependencies",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "serve": "python3 -m http.server 8000"
  },
  "license": "MIT"
}
